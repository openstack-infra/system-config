- job:
    name: check-gerrit-unittests
    project-type: maven
    node: precise

    wrappers:
      - timeout:
          timeout: 40
          fail: true
      - timestamps

    prebuilders:
      - gerrit-git-prep
      - gerrit-preclean

    maven:
      maven-name: Maven3
      root-module:
        group-id: com.google.gerrit
        artifact-id: gerrit-parent
      goals: 'clean package -Dgerrit.include-documentation=1 -X -B'

    postbuilders:
      - gerrit-postrun

    publishers:
      - war:
          site: 'tarballs.openstack.org'
          warfile: 'gerrit-war/target/gerrit*.war'
          target: 'tarballs/ci/test/'
      - console-log


- job:
    name: gate-gerrit-unittests
    project-type: maven
    node: precise

    wrappers:
      - timeout:
          timeout: 40
          fail: true
      - timestamps

    prebuilders:
      - gerrit-git-prep
      - gerrit-preclean

    maven:
      maven-name: Maven3
      root-module:
        group-id: com.google.gerrit
        artifact-id: gerrit-parent
      goals: 'clean package -Dgerrit.include-documentation=1 -X -B'

    postbuilders:
      - gerrit-postrun

    publishers:
      - console-log

- job:
    name: gerrit-package
    project-type: maven
    node: precise

    wrappers:
      - timeout:
          timeout: 40
          fail: true
      - timestamps

    prebuilders:
      - gerrit-git-prep
      - gerrit-preclean

    maven:
      maven-name: Maven3
      root-module:
        group-id: com.google.gerrit
        artifact-id: gerrit-parent
      goals: 'clean package -Dgerrit.include-documentation=1 -X -B'

    postbuilders:
      - gerrit-postrun

    publishers:
      - war:
          site: 'tarballs.openstack.org'
          warfile: 'gerrit-war/target/gerrit*.war'
          target: 'tarballs/ci/gerrit'
      - console-log

- job:
    name: check-gerrit-buck
    node: precise

    wrappers:
      - timeout:
          timeout: 40
          fail: true
      - timestamps

    builders:
      - gerrit-git-prep
      - shell: |
          #!/bin/bash -xe
          /usr/local/jenkins/slave_scripts/maven-properties.sh
      - inject:
          properties-file: maven.properties
      - shell: |
          #!/bin/bash -xe
          buck clean
          buck build release
          buck test --all
      - shell: "cp buck-out/gen/release.war buck-out/gen/gerrit-$PROJECT_VER.war"

    publishers:
      - war:
          site: 'tarballs.openstack.org'
          warfile: 'buck-out/gen/gerrit-${{PROJECT_VER}}.war'
          target: 'tarballs/ci/test'
      - console-log

- job:
    name: gate-gerrit-buck
    node: precise

    wrappers:
      - timeout:
          timeout: 40
          fail: true
      - timestamps

    builders:
      - gerrit-git-prep
      - shell: |
          #!/bin/bash -xe
          /usr/local/jenkins/slave_scripts/maven-properties.sh
      - inject:
          properties-file: maven.properties
      - shell: |
          #!/bin/bash -xe
          buck clean
          buck build release
          buck test --all
      - shell: "cp buck-out/gen/release.war buck-out/gen/gerrit-$PROJECT_VER.war"

    publishers:
      - console-log

- job:
    name: gerrit-package-buck
    node: precise

    wrappers:
      - timeout:
          timeout: 40
          fail: true
      - timestamps

    builders:
      - gerrit-git-prep
      - shell: |
          #!/bin/bash -xe
          /usr/local/jenkins/slave_scripts/maven-properties.sh
      - inject:
          properties-file: maven.properties
      - shell: |
          #!/bin/bash -xe
          buck clean
          buck build release
      - shell: "cp buck-out/gen/release.war buck-out/gen/gerrit-$PROJECT_VER.war"

    publishers:
      - war:
          site: 'tarballs.openstack.org'
          warfile: 'buck-out/gen/gerrit-${{PROJECT_VER}}.war'
          target: 'tarballs/ci/gerrit'
      - console-log
