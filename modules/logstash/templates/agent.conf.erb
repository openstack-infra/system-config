input {
  stdin {
    type => "stdin-type"
  }
}

output {
  redis {
    host => "127.0.0.1"
    data_type => "list"
    key => "logstash"
    password => "<%= scope.lookupvar("::logstash::agent::redis_password") %>"
  }
}
