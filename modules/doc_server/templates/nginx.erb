server {
  listen 80;
  server_name <%= name %>.openstack.org;
  root /srv/docs/<%= name %>;
  location ^~ /docs/ {
    alias /srv/docs/<%= name %>/trunk;
  }
  location ^~ /tarballs/ {
    alias /srv/tarballs/<%= name %>;
  }
}
