- job-template:
    name: 'opencontrail-{manual}'
    project-type: maven
    node: bare-precise
    concurrent: false

    prebuilders:
      - gerrit-git-prep

    properties:
      - inject:
          script-file: /usr/local/jenkins/slave_scripts/docbook-properties.sh
          properties-file: gerrit-doc.properties

    maven:
      maven-name: Maven3
      root-module:
        group-id: org.opencontrail.docs
        artifact-id: opencontrail-guide
      root-pom: doc/{manual}/pom.xml
      goals: '-Drelease.path.name=${{DOC_RELEASE_PATH}} -Dcomments.enabled=${{DOC_COMMENTS_ENABLED}} clean generate-sources -B'

    publishers:
      - console-log
      - ftp:
          site: docs.opencontrail.org
          source: 'doc/{manual}/target/docbkx/webhelp/**'
          target: ''
          remove-prefix: 'doc/{manual}/target/docbkx/webhelp/'
          excludes: '**/*.xml,**/null*'
      - ftp:
          site: docs.opencontrail.org
          source: 'doc/{manual}/target/docbkx/webhelp/**/atom.xml'
          target: ''
          remove-prefix: 'doc/{manual}/target/docbkx/webhelp/'

- job-template:
    name: 'opencontrail-install-deploy-guide-{distribution}'

    project-type: maven
    concurrent: false
    node: bare-precise

    properties:
      - inject:
          script-file: /usr/local/jenkins/slave_scripts/docbook-properties.sh
          properties-file: gerrit-doc.properties

    prebuilders:
      - gerrit-git-prep

    maven:
      maven-name: Maven3
      root-module:
        group-id: org.opencontrail.docs
        artifact-id: opencontrail-guide
      root-pom: doc/install-guide/pom.xml
      goals: '-Doperating.system={system} -Dprofile.os={profileos} -Drelease.path.name=${{DOC_RELEASE_PATH}} -Dcomments.enabled=${{DOC_COMMENTS_ENABLED}} clean generate-sources -B'

    publishers:
      - console-log
      - ftp:
          site: docs.opencontrail.org
          source: 'doc/install-guide/target/docbkx/webhelp/**'
          target: '/'
          remove-prefix: 'doc/install-guide/target/docbkx/webhelp/'
          excludes: '**/*.xml,**/null*'
      - ftp:
          site: docs.opencontrail.org
          source: 'doc/install-guide/target/docbkx/webhelp/**/atom.xml'
          target: '/'
          remove-prefix: 'doc/install-guide/target/docbkx/webhelp/'

- job-template:
    name: 'opencontrail-install-deploy-guide-{lang}'

    concurrent: false
    node: bare-precise

    properties:
      - inject:
          script-file: /usr/local/jenkins/slave_scripts/docbook-properties.sh
          properties-file: gerrit-doc.properties

    builders:
      - gerrit-git-prep
      - shell: |
          source /usr/local/jenkins/slave_scripts/select-mirror.sh opencontrail opencontrail-manuals
          tox -v -e buildlang-install -- {lang}

    publishers:
      - console-log
      - ftp:
          site: docs.opencontrail.org
          source: 'publish-docs/{lang}/**'
          target: '/{lang}/'
          remove-prefix: 'publish-docs/{lang}/'

- job-template:
    name: opencontrail-{oldmanual}-old
    project-type: maven
    node: bare-precise
    concurrent: false

    prebuilders:
      - gerrit-git-prep

    properties:
      - inject:
          script-file: /usr/local/jenkins/slave_scripts/docbook-properties.sh
          properties-file: gerrit-doc.properties

    maven:
      maven-name: Maven3
      root-module:
        group-id: org.opencontrail.docs
        artifact-id: opencontrail-guide
      root-pom: doc/src/docbkx/opencontrail-{oldmanual}/pom.xml
      goals: '-Drelease.path.name=${{DOC_RELEASE_PATH}} -Dcomments.enabled=${{DOC_COMMENTS_ENABLED}} clean generate-sources -B'

    publishers:
      - console-log
      - ftp:
          site: docs.opencontrail.org
          source: 'doc/src/docbkx/opencontrail-{oldmanual}/target/docbkx/webhelp/**'
          target: '/'
          remove-prefix: 'doc/src/docbkx/opencontrail-{oldmanual}/target/docbkx/webhelp/'
          excludes: '**/*.xml,**/null*'
      - ftp:
          site: docs.opencontrail.org
          source: 'doc/src/docbkx/opencontrail-{oldmanual}/target/docbkx/webhelp/**/atom.xml'
          target: '/'
          remove-prefix: 'doc/src/docbkx/opencontrail-{oldmanual}/target/docbkx/webhelp/'

- job-template:
    name: opencontrail-{install}-{distribution}-old
    project-type: maven
    node: bare-precise
    concurrent: false

    prebuilders:
      - gerrit-git-prep

    properties:
      - inject:
          script-file: /usr/local/jenkins/slave_scripts/docbook-properties.sh
          properties-file: gerrit-doc.properties

    prebuilders:
      - gerrit-git-prep

    maven:
      maven-name: Maven3
      root-module:
        group-id: org.opencontrail.docs
        artifact-id: opencontrail-guide
      root-pom: doc/src/docbkx/{install}/pom.xml
      goals: '-Doperating.system={system} -Dprofile.os={profileos} -Drelease.path.name=${{DOC_RELEASE_PATH}} -Dcomments.enabled=${{DOC_COMMENTS_ENABLED}} clean generate-sources -B'

    publishers:
      - console-log
      - ftp:
          site: docs.opencontrail.org
          source: 'doc/src/docbkx/{install}/target/docbkx/webhelp/**'
          target: ''
          remove-prefix: 'doc/src/docbkx/{install}/target/docbkx/webhelp/'
          excludes: '**/*.xml,**/null*'
      - ftp:
          site: docs.opencontrail.org
          source: 'doc/src/docbkx/{install}/target/docbkx/webhelp/**/atom.xml'
          target: ''
          remove-prefix: 'doc/src/docbkx/{install}/target/docbkx/webhelp/'

- job-template:
    name: 'gate-{name}-tox-doc-publish-{envlist}'

    wrappers:
      - timeout:
          timeout: 40
          fail: true
      - timestamps

    builders:
      - gerrit-git-prep
      - tox:
          envlist: '{envlist}'
          github-org: '{github-org}'
          project: '{name}'
      - assert-no-extra-files

    publishers:
      - console-log
      - upload-docs-draft
      - upload-doc-build-results

    node: '{node}'

- job-group:
    name: opencontrail-doc-jobs
    jobs:
      - gate-{name}-tox-{envlist}:
          envlist: checkniceness
      - gate-{name}-tox-{envlist}:
          envlist: checksyntax
      - gate-{name}-tox-{envlist}:
          envlist: checkdeletions
      - gate-{name}-tox-doc-publish-{envlist}:
          envlist: checkbuild
