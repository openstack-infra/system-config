- job-template:
    name: 'ci-{name}-build'
    node: juniper-tests

    wrappers:
      - timeout:
          timeout: 600
          fail: true
      - timestamps

    builders:
      - gerrit-git-prep
      - shell: |
          #!/bin/bash -xe
          export USER=jenkins
          export JOB_NAME={name}
          contrail-ci-run /usr/local/jenkins/slave_scripts/contrail-unittests-job.sh

- job-group:
    name: contrail-controller-jobs
    jobs:
      - 'ci-{name}-build'

- job-group:
    name: contrail-ci-test-jobs
    jobs:
      - 'ci-{name}-build'

- job-group:
    name: contrail-controller-test-jobs
    jobs:
      - 'ci-{name}-build'
