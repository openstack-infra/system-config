# Not using the template because opencontrail-planet does not use tox.
- job:
    name: 'gate-opencontrail-planet-unittest'
    node: bare-precise

    builders:
      - gerrit-git-prep
      - shell: |
          python test.py

    publishers:
      - console-log
