apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitea
  namespace: gitea
  labels:
    app: gitea
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gitea
  template:
    metadata:
      labels:
        app: gitea
    spec:
      initContainers:
        - name: gitea-config
          image: jeblair/jinja-init
          env:
            - {name: JINJA_SRC_FILE, value: /config_src/app.ini.j2}
            - {name: JINJA_DEST_FILE, value: /conf/app.ini}
            - {name: VERBOSE, value: '1'}
          volumeMounts:
            - {name: config-template, mountPath: /config_src}
            - {name: gitea-conf, mountPath: /conf}
            - {name: secrets, mountPath: /secrets}
      containers:
        - name: gitea
          image: jeblair/gitea #gitea/gitea:1.6.0
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: gitea-data
              mountPath: /data
            - name: gitea-conf
              mountPath: /custom/conf
            - name: logs
              mountPath: /logs
        - name: openssh
          image: jeblair/gitea-openssh
          ports:
            - containerPort: 22
          volumeMounts:
            - name: gitea-data
              mountPath: /data
            - name: gitea-conf
              mountPath: /custom/conf
      volumes:
        - name: gitea-data
          flexVolume:
            driver: ceph.rook.io/rook
            fsType: ceph
            options:
              fsName: rookfs
              clusterNamespace: rook-ceph
              clusterName: rook-ceph
        - name: config-template
          configMap:
            name: gitea-conf
        - name: gitea-conf
          emptyDir:
        - name: logs
          emptyDir:
        - name: secrets
          secret:
            secretName: gitea-app
