- hosts: bridge.openstack.org
  tasks:
    - name: Collect testinfra logs
      when: item is file
      synchronize:
        dest: "{{ zuul.executor.log_root }}"
        mode: pull
        src: "{{ item }}"
        verify_host: true
      loop:
        - "{{ zuul.project.src_dir }}/junit.xml"
