- name: Set up additional repos
  include_tasks: "{{ found_tasks }}"
  when: found_tasks
  vars:
    params:
      files:
        - "{{ ansible_facts.distribution }}.yaml"
        - "{{ ansible_facts.os_family }}.yaml"
      skip: true
    found_tasks: "{{ lookup('first_found', params) }}"
